version: '3.8'

services:
  backend:
    environment:
      - NODE_ENV=staging
    command: sh -c "node dist/scripts/seed-staging.js 2>/dev/null || true && node dist/index.js"
  
  postgres:
    ports:
      - "5432:5432"
